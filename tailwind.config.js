/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ["./src/**/*.{html,js}"],  //giving this tailind will support to the all the files such as html,js etc. Anywhere under the src folder whatever files have the extentions of(HTML,JS,CSS) there this tailind can use.
 //above content line changed from default
  theme: {
    extend: {},
  },
  plugins: [],
}

